FORMAT PE GUI

SECTION '.CODE' CODE READABLE WRITEABLE EXECUTABLE

	CALL	ADE86

	PUSH	32
	PUSH	100H
	PUSH	SOURCE
	PUSH	BUFFER+80
	PUSH	BUFFER
	CALL	NEAR[EAX]
	ADD	ESP,4*5
	MOV	EAX,DWORD[BUFFER+8]
	RET

	BUFFER DB 80 DUP(0)
	SOURCE DB 'MOV EAX,[DWORD ESP+ECX*2+1000]',0

ADE86:
	FILE	'ADEINC.BIN'